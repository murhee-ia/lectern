# Regenerated wholesale by `supabase gen types typescript` — via the
# supabase:types script, and by deploy.yml on every merge to main. Prettier
# reformats it, the next regeneration reverts that, and the churn repeats 
# forever. The generator's output is the canonical formatting for this file.
#
# Build output (.next, .turbo, test-results, node_modules) needs no entry —
# Prettier's own defaults already ignore all of it.
packages/supabase/types.ts

# Regenerated by Next.js on every build; carries its own "do not edit" notice.
apps/*/next-env.d.ts
